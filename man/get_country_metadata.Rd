% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/country-metadata.R
\name{get_country_metadata}
\alias{get_country_metadata}
\title{Get country metadata (name, WHO region)}
\usage{
get_country_metadata(
  countries = NULL,
  cache = TRUE,
  quiet = TRUE,
  offline = FALSE
)
}
\arguments{
\item{countries}{Optional ISO3 character vector. If \code{NULL}, returns metadata
for all available countries.}

\item{cache}{Logical; passed to \code{\link[=gho_get]{gho_get()}}.}

\item{quiet}{Logical; passed to \code{\link[=gho_get]{gho_get()}}.}

\item{offline}{Logical; if \code{TRUE}, attempt to use a pinned fixture instead of
the live API.}
}
\value{
A tibble with columns:
\itemize{
\item \code{iso3}: ISO3 country code
\item \code{country}: country name
\item \code{who_region}: WHO region code (e.g. "AFR")
\item \code{who_region_name}: WHO region name (e.g. "Africa")
}
}
\description{
Retrieve metadata for countries from the WHO GHO OData \emph{dimension values}.
This is useful for joining human-readable country names and WHO region codes
onto country-year panels.
}
\details{
By default this function uses the live API (with caching via \code{\link[=gho_get]{gho_get()}}).
For deterministic vignettes/tests, you can set \code{offline = TRUE} to use a
pinned fixture (when available).
}
