% Generated by hand (subagent); normally via roxygen2
\name{get_country_metadata}
\alias{get_country_metadata}
\title{Get country metadata (name, WHO region)}
\usage{
get_country_metadata(countries = NULL, cache = TRUE, quiet = TRUE, offline = FALSE)
}
\arguments{
\item{countries}{Optional ISO3 character vector. If \code{NULL}, returns metadata for all available countries.}
\item{cache}{Logical; passed to \code{gho_get()}.}
\item{quiet}{Logical; passed to \code{gho_get()}.}
\item{offline}{Logical; if \code{TRUE}, use a pinned fixture instead of calling the live API.}
}
\value{
A tibble with columns \code{iso3}, \code{country}, \code{who_region}, \code{who_region_name}.
}
\description{
Retrieve metadata for countries from the WHO GHO OData dimension values.
This is useful for joining human-readable country names and WHO region codes
onto country-year panels.
}
